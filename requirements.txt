# BTC Cross-Matrix Puzzle Scanner Dependencies
# Install with: pip install -r requirements.txt

# Fast C library for Bitcoin operations (optional but recommended)
coincurve>=18.0.0

# For .env file support (optional)
python-dotenv>=1.0.0

# HTTP requests for Telegram
requests>=2.28.0

# Pure Python fallback (always works)
ecdsa>=0.18.0
